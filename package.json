{
  "name": "pouch-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "tauri dev",
    "start": "cargo run --release --manifest-path src-tauri/Cargo.toml",
    "build": "tauri build --no-bundle",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
